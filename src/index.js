import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// git remote add origin https://github.com/kottaips/ganesh_aws_amplify.git

// git add . 
// git branch -M main
// git push -u origin main